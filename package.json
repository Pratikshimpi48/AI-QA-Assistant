{
  "name": "ai-qa-assistant-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --include=dev --prefix client && npm run build --prefix client && npm install --prefix server && node scripts/copy-dist.js",
    "start": "npm run start --prefix server",
    "db:view": "node server/scripts/view-db.js"
  }
}
